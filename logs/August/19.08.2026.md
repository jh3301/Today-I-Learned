Log entry: 19th August 2026

Today I moved on to managing file content, searching, and file system manipulation in the Bash shell.

- I learned how to use the grep command to search for specific strings inside files and looked more closely at how arguments customise command behaviour.

- I explored standard output and standard input through piping (|), learning how to redirect the output of one command to serve directly as the input for another.

- I was introduced to the find command for locating files across the system, using options like -name and -iname to perform case-sensitive and case-insensitive filename searches.

- I moved on to creating, deleting, and modifying files and directories, practising with mkdir, rmdir, touch, rm, cp, and mv.

- I was introduced to the nano text editor to create, edit, and save text files directly from the command line.

- I completed two hands-on Qwiklabs activities to put everything into practice:

- In the first lab, I used grep to search for lines containing the word "error" in files, and combined grep with pipes (|) to filter and output specific matching strings.

- In the second lab, I practised directory and file management by removing a directory with rmdir, creating a new one with mkdir, creating a file with touch, deleting a file with rm, moving a file between directories using mv, and editing text inside the new file using nano.

Moving forward, I want to keep practising chaining these search and manipulation commands together to get faster at parsing logs and navigating files.
