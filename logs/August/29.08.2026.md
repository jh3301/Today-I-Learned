Log entry 29th August 2026

Today I started learning how to apply basic filters to query databases using SQL.

- I learned how to use the `WHERE` clause to filter query results based on specific conditions.
- I practised using the `=` operator to search for exact matches in fields.
- I explored pattern matching using the `LIKE` operator alongside wildcards:
  - Used the `%` wildcard to represent zero, one, or multiple characters.
  - Used the `_` wildcard to represent a single, specific character space.
- I looked at how these wildcards help search for partial strings, prefixes, and suffixes across database records when exact values are unknown.
- I completed a hands-on Qwiklabs activity where I put all these techniques into practice, writing queries using `WHERE`, `LIKE`, `=`, and wildcards to filter specific data from tables.

Moving forward, I want to practice combining multiple conditions using logical operators to refine my data filtering further.
